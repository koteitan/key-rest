/**
 * Creates a WebSocket-compatible constructor that routes connections through key-rest-daemon.
 * TODO: Implement when daemon WebSocket support is added.
 */
export function createWebSocket() {
  throw new Error('WebSocket support is not yet implemented');
}
