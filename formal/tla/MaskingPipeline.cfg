SPECIFICATION Spec

\* Positive configuration: all covered encodings, fixed decoder, correct snapshot, step 4 on.
\* Expected result: No error found.

CONSTANTS
    ServerEncodings = { "none", "raw", "json_esc", "b64",
                        "pct_raw", "pct_b64", "pct_stray", "pct_plus_b64", "trunc" }
    ExtraStep4Catches = { "pct_stray", "pct_plus_b64" }
    UseSnapshot = TRUE
    Step4Enabled = TRUE

INVARIANT TypeOK
INVARIANT NoCredentialLeak
INVARIANT ResolvedImpliesAllowed
